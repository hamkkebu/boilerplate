# ========================================================
# 기본: node_modules, 빌드 결과물, 환경 설정 등 제외
# ========================================================
node_modules
dist
build
coverage
.cache
.tmp

# ========================================================
# 환경 변수, 로그, 의존성 캐시 등 제외
# ========================================================
.env
.env.*
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ========================================================
# Git 관련 파일 (리포 이력은 Docker 빌드에 불필요)
# ========================================================
.git
.gitignore
.gitattributes

# ========================================================
# OS / IDE 관련 파일들
# ========================================================
.DS_Store
Thumbs.db
*.swp
*.swo
.vscode
.idea
*.iml

# ========================================================
# Docker 자체 파일 (빌드 컨텍스트에 불필요)
# ========================================================
Dockerfile
Dockerfile.*
docker-compose.yml
